"use client"

import { useState } from "react"

export default function RequestsTable() {
  // Mock data - in a real app, this would come from API
  const [requests] = useState([
    // ... mock data ...
  ])

  // No data fetching or revalidation logic
}
